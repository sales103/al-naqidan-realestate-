{
  "name": "09 - تذكيرات المواعيد",
  "nodes": [
    {
      "parameters": {
        "rule": {
          "interval": [{ "field": "cronExpression", "expression": "0 9 * * *" }]
        }
      },
      "id": "schedule",
      "name": "تشغيل يومي 9 صباحاً",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1,
      "position": [240, 300]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ $env.BACKEND_URL }}/api/appointments/reminders",
        "authentication": "headerAuth",
        "options": {}
      },
      "id": "send-reminders",
      "name": "إرسال تذكيرات المواعيد",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4,
      "position": [460, 300]
    }
  ],
  "connections": {
    "تشغيل يومي 9 صباحاً": {
      "main": [[{ "node": "إرسال تذكيرات المواعيد", "type": "main", "index": 0 }]]
    }
  },
  "settings": { "executionOrder": "v1" },
  "tags": [{ "name": "appointments" }]
}
